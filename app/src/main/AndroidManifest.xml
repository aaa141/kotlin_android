<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlintest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinTest">
        <!-- Chapter1: BMI 계산기 만들기 -->
        <activity android:name=".basic.b1_BMIcalc" />
        <activity android:name=".basic.b1_ResultActivity" />
        <!-- Chapter2: 로또 추첨기 만들기 -->
        <activity android:name=".basic.b2_LottoPicker"></activity>
        <activity
            android:name=".basic.b3_SecretDiary"
            android:theme="@style/Theme.KotlinTest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".basic.b3_DiaryActivity"
            android:theme="@style/Theme.KotlinTest.NoActionBar"/>

        <!-- Chapter6: 뽀모도로 타이머 -->
        <activity android:name=".basic.b6_PomodoroTimer" />
    </application>

</manifest>